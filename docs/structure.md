# InfoScreen repository layout

The canonical repository structure, runtime-path, logging, static-asset, systemd, and change-scope constraints are maintained in:

```text
docs/project-structure.md
```

Do not duplicate structure rules in this file. If a structure or runtime-path rule needs to change, update `docs/project-structure.md` first, then keep this file as a pointer.
