name: InfoScreen acceptance

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  acceptance:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install acceptance dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pydantic

      - name: Run acceptance checks
        env:
          ACCEPTANCE_START_SERVER: '1'
          ACCEPTANCE_ARTIFACT_DIR: /tmp/infoscreen-acceptance
        run: bash scripts/run_acceptance.sh

      - name: Upload acceptance artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: infoscreen-acceptance-${{ github.sha }}
          path: /tmp/infoscreen-acceptance
          if-no-files-found: warn
          retention-days: 14
