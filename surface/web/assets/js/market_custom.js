(function () {
  "use strict";
  // Kiosk mode must not inject the market editor into the dashboard panel.
  // Market symbols remain configurable through /api/market-config and refresh through /api/market-refresh.
})();
